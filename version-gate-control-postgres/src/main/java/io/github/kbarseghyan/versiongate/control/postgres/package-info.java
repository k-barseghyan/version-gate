/**
 * PostgreSQL authoritative-store adapter boundary.
 *
 * <p>The module is intentionally a placeholder. A future implementation may provide the single
 * durable {@code VersionGateStore} persistence boundary without changing the SPI or core.
 */
package io.github.kbarseghyan.versiongate.control.postgres;
