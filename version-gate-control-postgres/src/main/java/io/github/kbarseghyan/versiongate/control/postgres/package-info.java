/**
 * PostgreSQL control-store adapter boundary.
 *
 * <p>The module is intentionally a placeholder during the multi-module refactor. A concrete
 * implementation will provide a durable {@code ControlStore} without changing the SPI or core.
 */
package io.github.kbarseghyan.versiongate.control.postgres;
