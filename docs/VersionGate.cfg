SPECIFICATION Spec

CONSTANTS
  SessionIds = {s1, s2, s3}
  OperationIds = {o1, o2, o3, o4}
  Payloads = {p1, p2}
  TimeValues = {0, 1, 2}
  NoSession = noSession
  NoRecovery = noRecovery
  NoResolution = noResolution
  NoPayload = noPayload
  NoTerminalOutcome = noTerminalOutcome

SYMMETRY ModelSymmetry

INVARIANT TypeOK
INVARIANT SessionIdentitySafety
INVARIANT OpenDeadlineSafety
INVARIANT CoordinationSafety
INVARIANT VersionSafety
INVARIANT SnapshotSafety
INVARIANT HistorySafety
INVARIANT OperationReplySafety
INVARIANT TerminalSnapshotSubmissionSafety
INVARIANT SnapshotReadSafety
INVARIANT FenceSafety
PROPERTY TemporalSafety

\* Exhaustion of the finite TLC domains is a model bound, not a business
\* deadlock. The normative algorithm also permits indefinite stuttering.
CHECK_DEADLOCK FALSE
